# Generated / vendored
node_modules
.next
out
build
package-lock.json
next-env.d.ts

# Machine-generated eval artifacts (JSON.stringify output, committed for /evals
# rendering per EVAL-16; regenerated each run, so not hand-formatted). The
# hand-authored evals/testset.json is NOT ignored.
evals/runs/
evals/latest.json
evals/calibration.json
evals/baseline.json

# Binary + third-party
*.woff2
app/fonts/LICENSE.md

# No prettier parser
.env.example

# GitHub Actions YAML (the `on:` key is a YAML 1.1 boolean; validated by GitHub, not prettier)
.github

# Brandon's frozen specs and operating contract (never reformat)
.claude
CLAUDE.md
