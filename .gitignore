# dependencies
/node_modules
/.pnp
.pnp.*

# testing / coverage
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files: secrets never committed (SEC-01); .env.example IS committed (ENG-11)
.env*
!.env.example

# vercel
.vercel

# typescript build info (auto-generated). next-env.d.ts is intentionally
# committed so `tsc --noEmit` has Next ambient types before `next build` runs.
*.tsbuildinfo
